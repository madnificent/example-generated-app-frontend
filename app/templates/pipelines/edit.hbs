{{!-- Page to edit pipeline --}}
{{#with (changeset model skipValidate=true) as |changeset|}}
<div class="edit">
  <h2 class="title">{{t (humanize 'Edit pipeline')}}</h2>

  {{!--
    Actions to save or cancel changes;
    or delete pipeline.
  --}}
  <div class="actions">
    <button type="button" class="btn save" title="Save new pipeline" {{action 'save' changeset model}}>Save</button>
    <button type="button" class="btn cancel" title="Cancel" {{action 'cancel' changeset model}}>Cancel</button>
    <button type="button" class="btn delete" title="Delete this item" {{action 'delete' changeset model}}>Delete</button>
  </div>

  {{!-- Table to show details of pipeline. --}}
  <table>
    <thead>
      <tr>
        <th>Property</th>
        <th>Data</th>
      </tr>
    </thead>
    <tbody>
      {{!-- Creating rows for attributes depending on the type of property --}}
            <tr>
      <td>title</td>
      <td align='middle'>{{handle-attribute model=changeset attribute='title' editable=true}}</td>
</tr>
      <tr>
      <td>description</td>
      <td align='middle'>{{handle-attribute model=changeset attribute='description' editable=true}}</td>
</tr>

        <tr><td>dockerFile</td><td align='middle'>{{edit-belongs-to relName=changeset.dockerFile relType="docker-compose"}}</td></tr>
        <tr><td>steps</td><td align='middle'>{{edit-has-many relName=changeset.steps relType="step"}}</td></tr>

    </tbody>
  </table>
</div>
{{/with}}
